# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.standalone.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/messaging/webserver.pid;
error_log /var/log/sdkwork/messaging/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    include snippets/adaptive-web.maps.conf;
    upstream gateway {
        least_conn;
        keepalive 32;
        server 127.0.0.1:3900;
    }
    server {
        listen 80;
        server_name messaging-test.sdkwork.com messaging-test.birdcoder.com messaging-test.dtupay.com messaging-test.sdkwork.cn messaging-test.birdcoder.cn messaging-test.dtupay.cn messaging-test.skubc.com messaging-test.skubc.cn messaging-test.zowalk.com messaging-test.zowalk.cn messaging-test.offer86.com messaging-test.offer86.cn messaging-test.86offer.com messaging-test.86offer.cn;
        include snippets/gateway-locations.nonproduction.conf;
    }
}
