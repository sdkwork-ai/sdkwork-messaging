[package]
name = "sdkwork-messaging-delivery-service"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "SDKWork messaging domain contracts for external SMS and email delivery."

[lib]
name = "sdkwork_messaging_delivery_service"
path = "src/lib.rs"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
